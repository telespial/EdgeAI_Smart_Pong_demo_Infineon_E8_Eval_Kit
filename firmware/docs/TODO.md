# TODO

- Run `./tools/preflight.sh` and confirm all required files are present.
- Execute first local build and validate actual artifact path.
- Execute first local flash and confirm board runtime behavior.
- Update `docs/PROJECT_STATE.md` with validated build/flash commands and binary path.
